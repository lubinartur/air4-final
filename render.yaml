services:
  - type: web
    name: ar4gpt
    runtime: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: OPENROUTER_API_KEY
        value: sk-or-... # твой ключ, но лучше задать его в UI Render как secret
