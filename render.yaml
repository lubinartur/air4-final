services:
  - type: web
    name: ar4gpt
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: PORT
        value: 10000
      - key: OPENAI_API_KEY
        value: your_openai_key_here  # <--- замени на свой ключ
